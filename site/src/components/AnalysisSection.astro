---
const analyses = [
  {
    tag: 'Analyse technique',
    tagColor: 'bg-gold/10 text-gold border-gold/20',
    title: 'BTC/USD : le support à 95K$ tient bon, objectif 120K$',
    excerpt: 'Les indicateurs on-chain pointent vers une accumulation massive des whales. Notre analyse technique complète avec les niveaux clés à surveiller.',
    author: { name: 'Marc Lefebvre', role: 'Analyste Senior', initials: 'ML' },
    time: '12 min de lecture',
    premium: true,
  },
  {
    tag: 'Macro',
    tagColor: 'bg-info/10 text-info border-info/20',
    title: 'Fed, inflation et crypto : la corrélation qui change tout en 2026',
    excerpt: 'Comment les décisions de la Fed impactent directement les marchés crypto. Décryptage des mécanismes et anticipation des prochains mouvements.',
    author: { name: 'Alice Chen', role: 'Économiste', initials: 'AC' },
    time: '9 min de lecture',
    premium: false,
  },
  {
    tag: 'DeFi Deep Dive',
    tagColor: 'bg-positive/10 text-positive border-positive/20',
    title: 'Yield farming en 2026 : stratégies avancées et gestion du risque',
    excerpt: 'Au-delà des APY flashy, les stratégies durables qui génèrent du rendement réel. Pools, vaults et protocoles incontournables.',
    author: { name: 'Thomas Petit', role: 'Expert DeFi', initials: 'TP' },
    time: '15 min de lecture',
    premium: true,
  },
];
---

<section class="py-section">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-12 gap-10">
      <!-- Main Analyses -->
      <div class="lg:col-span-8">
        <div class="flex items-center justify-between mb-8">
          <div class="flex items-center gap-3">
            <div class="w-1 h-6 bg-gradient-to-b from-gold to-violet rounded-full"></div>
            <h2 class="font-display font-700 text-h2">Analyses & Opinions</h2>
          </div>
          <a href="/analyses" class="text-sm text-text-secondary hover:text-gold transition-colors duration-200 cursor-pointer flex items-center gap-1">
            Toutes les analyses
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>

        <div class="space-y-5">
          {analyses.map((article) => (
            <article class="group cursor-pointer rounded-card border border-border hover:border-text-muted bg-surface-raised/30 p-6 transition-all duration-300 hover:shadow-card">
              <a href="#" class="block">
                <div class="flex items-center gap-3 mb-3">
                  <span class={`inline-flex items-center px-2.5 py-0.5 text-xs font-semibold border rounded-full ${article.tagColor}`}>
                    {article.tag}
                  </span>
                  {article.premium && (
                    <span class="inline-flex items-center px-2 py-0.5 text-[10px] font-bold bg-gold/10 text-gold border border-gold/20 rounded-full uppercase tracking-wider">
                      Premium
                    </span>
                  )}
                  <span class="text-xs text-text-muted ml-auto font-mono">{article.time}</span>
                </div>
                <h3 class="font-display font-700 text-h3 mb-2 group-hover:text-gold transition-colors duration-200 leading-snug">
                  {article.title}
                </h3>
                <p class="text-sm text-text-secondary leading-relaxed mb-4">
                  {article.excerpt}
                </p>
                <div class="flex items-center gap-3 pt-3 border-t border-border-subtle">
                  <div class="w-8 h-8 rounded-full bg-gradient-to-br from-gold/50 to-violet/50 flex items-center justify-center text-[10px] font-bold text-text-primary">
                    {article.author.initials}
                  </div>
                  <div>
                    <p class="text-sm font-medium">{article.author.name}</p>
                    <p class="text-xs text-text-muted">{article.author.role}</p>
                  </div>
                </div>
              </a>
            </article>
          ))}
        </div>
      </div>

      <!-- Sidebar: Trending -->
      <aside class="lg:col-span-4">
        <div class="sticky top-32">
          <!-- Trending Articles -->
          <div class="rounded-card border border-border bg-surface-raised/30 p-6 mb-6">
            <h3 class="font-display font-700 text-body-lg mb-5 flex items-center gap-2">
              <svg class="w-5 h-5 text-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.362 5.214A8.252 8.252 0 0112 21 8.25 8.25 0 016.038 7.048 6.51 6.51 0 009 4.572" />
              </svg>
              Tendances
            </h3>
            <ol class="space-y-4">
              {[
                { num: '01', title: 'Pourquoi Solana pourrait dépasser Ethereum en TVL', cat: 'Analyse' },
                { num: '02', title: 'Airdrop saison 2026 : les 10 protocoles à surveiller', cat: 'Guide' },
                { num: '03', title: 'Trump et la crypto : bilan à mi-mandat', cat: 'Politique' },
                { num: '04', title: 'Le staking liquide change la donne pour les institutions', cat: 'DeFi' },
                { num: '05', title: 'Memcoins : bulle ou révolution culturelle ?', cat: 'Opinion' },
              ].map((item) => (
                <li>
                  <a href="#" class="group cursor-pointer flex gap-3 items-start">
                    <span class="text-2xl font-display font-800 text-border leading-none group-hover:text-gold/30 transition-colors duration-200">{item.num}</span>
                    <div>
                      <span class="text-[10px] font-semibold text-text-muted uppercase tracking-wider">{item.cat}</span>
                      <h4 class="text-sm font-medium leading-snug group-hover:text-gold transition-colors duration-200">{item.title}</h4>
                    </div>
                  </a>
                </li>
              ))}
            </ol>
          </div>

          <!-- Ad / Promo Card -->
          <div class="rounded-card border border-violet/20 bg-gradient-to-br from-violet/10 to-gold/5 p-6">
            <span class="text-[10px] font-semibold text-violet-light uppercase tracking-wider">Nouveau</span>
            <h3 class="font-display font-700 text-body-lg mt-2 mb-2">Portefeuille Crypto Pro</h3>
            <p class="text-sm text-text-secondary mb-4">Suivez vos investissements en temps réel avec nos outils d'analyse avancés.</p>
            <a href="#" class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium bg-violet text-white rounded-lg hover:bg-violet-light transition-colors duration-200 cursor-pointer">
              Découvrir
              <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</section>
