---
interface Props {
  totalGuides: number;
  totalBranches: number;
}

const { totalGuides, totalBranches } = Astro.props;
---

<section class="pt-32 pb-16 relative noise-overlay">
  <!-- Gradient background -->
  <div class="absolute inset-0 bg-gradient-to-b from-violet-dim/10 via-transparent to-transparent pointer-events-none"></div>

  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-1.5 mb-6 text-xs font-medium bg-gold/10 text-gold border border-gold/20 rounded-full">
      <span class="w-1.5 h-1.5 rounded-full bg-gold animate-pulse-dot"></span>
      Le guide Bitcoin francophone
    </div>

    <!-- H1 -->
    <h1 class="font-display font-800 text-4xl sm:text-5xl lg:text-6xl leading-[1.1] mb-5 tracking-tight">
      Comprendre, acheter et<br />investir dans <span class="text-gradient-gold">Bitcoin</span>
    </h1>

    <!-- Subtitle with dynamic stats -->
    <p class="text-lg sm:text-xl text-text-secondary leading-relaxed max-w-2xl mx-auto mb-8">
      {totalGuides}+ guides pratiques en {totalBranches} themes pour maitriser Bitcoin de A a Z. Gratuit, sans pub, 100% independant.
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href="/bitcoin"
        class="inline-flex items-center gap-2 px-6 py-3 text-sm font-semibold bg-gold text-void rounded-lg hover:bg-gold-light transition-colors cursor-pointer"
      >
        Explorer le guide Bitcoin
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
      <a
        href="/outils"
        class="inline-flex items-center gap-2 px-6 py-3 text-sm font-medium border border-border text-text-secondary rounded-lg hover:border-gold/40 hover:text-gold hover:bg-gold/5 transition-all cursor-pointer"
      >
        Voir les outils
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </a>
    </div>
  </div>
</section>
